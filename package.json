{

  "name": "test",
  "version": "0.1.0",
  "private": true,
  "proxy": "https://pkapi.astrid.fun",
  "dependencies": {
    "cookie-parser": "^1.4.4",
    "express": "^4.16.4",
    "node-fetch": "^2.3.0",
    "react": "^16.8.5",
    "react-cookies": "^0.1.0",
    "react-dom": "^16.8.5",
    "react-router-dom": "^5.0.0",
    "react-scripts": "2.1.8"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd frontend && npm install --only=dev && npm install && npm run build",
    "build": "cd frontend && yarn build && cd .. && node index.js"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]

}
